jack: JACK (Jack Audio Connection Kit)
jack:
jack: JACK is a low-latency audio server, written for POSIX conformant
jack: operating systems such as GNU/Linux and Apple's OS X.  It can connect
jack: a number of different applications to an audio device, as well as
jack: allowing them to share audio between themselves.  Its clients can run
jack: in their own processes (ie. as normal applications), or they can run
jack: within the JACK server (ie. as a "plugin").
jack: JACK was designed from the ground up for professional audio work,
jack: and its design focuses on two key areas: synchronous execution of all
jack: clients, and low latency operation.
jack: NOTE: This package provides both classic JACK and D-Bus JACK.
jack:
jack: Packaged by Georgi D. Sotirov <gdsotirov@dir.bg>
